# Plant Recognition Neural Network

A learning project to build and train a neural network from scratch in PyTorch,
to recognize plant species from images.

## Setup

1. Clone this repo
2. Create a virtual environment: `python3.11 -m venv venv`
3. Activate it: `source venv/bin/activate`
4. Install dependencies: `pip install -r requirements.txt`
